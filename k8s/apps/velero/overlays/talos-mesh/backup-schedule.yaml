apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: bitwarden-daily
  namespace: velero
spec:
  schedule: "0 3 * * *"
  template:
    ttl: 720h
    includedNamespaces:
    - bitwarden
    defaultVolumesToFsBackup: true
