apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: media-daily
  namespace: velero
spec:
  schedule: "0 4 * * *"
  template:
    ttl: 720h
    includedNamespaces:
    - media
    defaultVolumesToFsBackup: true
