apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Namespace + the spacetraders-pg-app secret are owned by the spacetraders app;
# this app only adds the Sim Lab deployment + service into that namespace.
resources:
- deployment.yaml
- service.yaml
